
@helper GetTreeView(List<ManufacturingManagement_V2.Models.Menu_MenuName> siteMenu, int parentID)
{
    foreach (var i in siteMenu.Where(a => a.ParentMenuId.Equals(parentID)))
    {
        <li>
            @{var submenu = siteMenu.Where(a => a.ParentMenuId.Equals(i.MenuId)).Count();}
            @if (submenu > 0)
            {
                <span class="collapse collapsible">&nbsp;</span>
            }
            else
            {
                <span style="width:15px; display:inline-block">&nbsp;</span>
            }
            <span>
                <a href="@i.NavUrl">@i.MenuName</a>
            </span>
            @if (submenu > 0)
            {
                <ul>
                    @Treeview.GetTreeView(siteMenu, i.MenuId)
                    @* Recursive  Call for Populate Sub items here*@
                </ul>
            }
        </li>
    }
}


@*@{
    Layout = null;
}
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <div>

    </div>
</body>
</html>*@
